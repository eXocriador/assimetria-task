# Shared configuration for docker-compose.prod.yml

# Database
POSTGRES_USER=blog
POSTGRES_PASSWORD=super-secure-password
POSTGRES_DB=blog

# Backend
PORT=4000
DB_HOST=db
DB_PORT=5432
DB_USER=${POSTGRES_USER}
DB_PASSWORD=${POSTGRES_PASSWORD}
DB_NAME=${POSTGRES_DB}
AI_PROVIDER=huggingface
HF_API_KEY=replace_with_real_token
HF_MODEL=mistralai/Mistral-7B-Instruct-v0.3

# Frontend
VITE_API_URL=http://your-ec2-public-dns:4000
